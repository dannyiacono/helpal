<%= simple_form_for([request, review]) do |form| %>
  <%= form.input :rating, as: :select, collection: 1..5 %>
  <%= form.input :content, as: :text, class: "review-text-input" %>
  <%= form.button :submit, class: "" %>
<% end %>
