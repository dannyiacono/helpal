<%= form_for [@conversation, @message] do |f| %>
  <div class=”field”>
   <%= f.text_area :body %>
  </div>
  <%= hidden_field_tag :user_id, current_user.id %>
  <div>
   <%= f.submit "Reply" %>
  </div>
 <% end %>
