<%= form_for [conversation, message] do |f| %>
  <div class=”field”>
   <%= f.text_area :body, placeholder: "Type your message here" %>
  </div>
  <div>
   <%= f.submit "Reply" %>
  </div>
 <% end %>
